{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" },
    { "src": "*.html", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/admin", "dest": "/admin.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
```

**Step 5** — Click **Commit new file**

---

## Can you share a screenshot of your GitHub repo?

That way I can see exactly what files you have and tell you precisely what to do. Your repo page looks like this:
```
github.com/yourusername/your-repo-name
